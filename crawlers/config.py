import json


APIFY_API_TOKEN = "###"
#ME#
OPENAI_TOKEN = "###"

cookies_path = "cookies.json"

# # Open the JSON file and load its content
# with open(cookies_path, "r", encoding="utf-8") as file:
#     COOKIES = json.load(file)  # Load the JSON data into a Python dictionary or list